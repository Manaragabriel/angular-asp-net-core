import { DataFormatoPipe } from './data-formato.pipe';

describe('DataFormatoPipe', () => {
  it('create an instance', () => {
    const pipe = new DataFormatoPipe();
    expect(pipe).toBeTruthy();
  });
});
